#!/bin/sh
. "$(dirname "$0")/husky.sh"

# 1) Lint/Prettier (루트 lint-staged 설정 사용)
npx lint-staged || exit 1

# 2) TS 타입체크 (frontend 앱 전체)
npm --prefix frontend --silent run typecheck || exit 1

# 3) FE 앱 테스트
npm --prefix frontend --silent run test || exit 1